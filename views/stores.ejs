<% layout('boilerplate/boilerplate.ejs') -%>
    <h1>All McDonald restaurant here</h1>
    <!-- <ul>
        <% for (let s of stores){ %>
            <li><a href="/stores/<%= s._id %> "><%= s.location %></a> </li>
        <% } %> 
    </ul>
    <a href="/stores/new">Create new McDonald</a> -->
    <div>
        <% for (let s of stores){ %>
            <div class ="card " >
                <div class = "row">
                    <div class="col-md-4">
                        <img class = "img-fluid" src=<%= s.image %>  alt="">
                    </div>
                    <div class="col-md-8">
                        <div class="card-body">
                            <h5 class="card-title"><%= s.location %> </h5>
                            <p class="card-text"><%= s.fact %> </p>
                            <a href="stores/<%= s._id %> " class="btn btn-primary">Details</a>
                        </div>
                    </div>
                </div>
            </div>
        <% } %> 
    </div>